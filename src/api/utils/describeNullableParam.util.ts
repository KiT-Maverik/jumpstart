export const describeNullableParam = (entity: string, parent: string) =>
	`Specify ID to get ${entity} for particular ${parent}; null to get ${entity} without ${parent}; no value to get all ${entity}.
				`
