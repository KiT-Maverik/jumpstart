import { IconButtonVariant, AlertMode } from '../types/theme'

export const alertModes: AlertMode[] = ['alert', 'banner'] as const

export const iconButtonVariants: IconButtonVariant[] = ['icon', 'outlined', 'contained'] as const
